import { FC, useState } from "react";
import styled from "styled-components";
import { Header, Main, Footer } from "@/components/layouts";
const SortableComponent: FC = () => {
  return (
    <>
      <Header />
      <Footer />
    </>
  );
};

export default SortableComponent;
